# Tier 1-3 Polish — Master Plan

> **STATUS: COMPLETE** — executed before 2026-05-14; preserved for audit trail. See `git log --oneline` for the commit sequence implementing this plan.

> Lock 2026-05-13 evening. Stephen approved all 11 items across the Tier 1-3 menu. Zero overlap with Vinh's lane. Estimated ~15-20 atomic commits.

## Tool inventory audit

Per locked memory policy [[tool-inventory-audit-per-task]]:

| Tool | Pass | Used? | Why |
|------|------|-------|-----|
| `general-purpose` research agent ×3 | Sub-agents | ✅ USED | Parallel research on (a) OSS-repo polish artifacts current versions, (b) Devpost image-gallery winning patterns, (c) Reddit Devvit last-30-days updates |
| `firecrawl` MCP | MCP | ✅ USED (via agents) | JS-rendered Reddit/Devpost/developers.reddit.com scraping |
| `banana` skill | Skills | ✅ USED | 3 Devpost image-gallery mockups (dashboard hero, mod menu, wiki config) |
| `plugin_playwright` MCP | MCP | ✅ USED | Architecture screenshot capture for gallery + OG-card unfurl test |
| `codex:codex-rescue` sub-agent | Sub-agents | ✅ USED | Final review across all polish commits |
| `frontend-design` skill | Skills | ✅ USED | Tier A UI polish principles applied |
| `caveman` skill | Skills | ✅ USED | Active |
| `karpathy-guidelines` skill | Skills | ✅ USED | Think/simplify/surgical per UI polish commit |
| `context7` MCP | MCP | ✅ USED (via agents) | Tailwind / Lucide official docs cross-check |

**SKIPPED with reason:**

| Tool | Skip reason |
|------|-------------|
| `magic` MCP | Magic returned 84KB inspiration data last session; over-applied per Codex feedback. Tier 1 #1 stays surgical (no component refinement). |
| `stitch` MCP | Existing project list is 51KB; gen takes 5+ min per screen. Banana 3-shot is faster + on-aesthetic. |
| `gemini-agent` | No long-context multi-file synthesis needed at polish layer. |
| `cc-gemini-plugin:gemini` | Same. |
| `claude_ai_Notion/Gmail/Calendar/Drive` | No project Notion/calendar/email surface. |
| `Stitch` design-system gen from DESIGN.md | Defer to Phase 3. Premature optimization at T-14d. |
| `pr-review-toolkit:silent-failure-hunter` | Already audited bash + JS in last cycle. |
| `pr-review-toolkit:comment-analyzer` | Already audited; new docs (CONTRIBUTING etc) will be content-only, not claims-vs-code. |
| `vercel:*` / `plugin_supabase` | N/A. |
| `mcp-builder` / `usage-audit` / `codeburn` | N/A. |

## File structure

Files this plan creates or modifies:

- Create: `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `CHANGELOG.md`
- Create: `.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/ISSUE_TEMPLATE/config.yml`
- Create: `.github/PULL_REQUEST_TEMPLATE.md`
- Create: `assets/gallery-dashboard.png`, `assets/gallery-modmenu.png`, `assets/gallery-wiki.png`, `assets/gallery-architecture.png`
- Modify: `tailwind.config.ts` (add `signal.author` token)
- Modify: `src/client/components/EventRow.tsx` (use `signal.author` token instead of hardcoded `#A78BFA`)
- Modify: `src/client/components/StatsRow.tsx` (hardcoded hex → token classes + pulse-dot for Active Rules card)
- Modify: `README.md` (v1.1 polish — realistic JSON5 example + install troubleshooting + FAQ)

## Wave sequence

### Wave A — Plan + research dispatch (this commit) ✓
- [x] **A.1** Master plan doc with tool inventory audit
- [x] **A.2** Three parallel research agents dispatched (in flight)

### Wave B — Tier 1 #1 UI polish (3 atomic commits, in flight before research lands)
- [ ] **B.1** `tailwind.config.ts` + `EventRow.tsx`: promote `#A78BFA` → `signal.author` token
- [ ] **B.2** `StatsRow.tsx`: replace hardcoded `accent` hex with Tailwind token classes
- [ ] **B.3** `StatsRow.tsx`: wire unused `pulse-dot` keyframe to "Active rules" card live indicator

### Wave C — Tier 1 #2 Devpost gallery (3 atomic commits)
- [ ] **C.1** Banana: dashboard hero mockup (1200×800 3:2)
- [ ] **C.2** Banana: mod-menu mockup with 3 menu items rendered (1200×800 3:2)
- [ ] **C.3** Banana: wiki-config code-rendering mockup (1200×800 3:2)
- [ ] **C.4** Playwright capture architecture diagram from README → crop to 3:2

### Wave D — Tier 1 #3 CHANGELOG (1 atomic commit)
- [ ] **D.1** Parse git log → group by category (Added / Changed / Fixed / Security / Docs) per keepachangelog v1.1.0 → commit `CHANGELOG.md`

### Wave E — Tier 2 public-repo polish (~5 atomic commits, informed by Wave A research)
- [ ] **E.1** `CODE_OF_CONDUCT.md` — Contributor Covenant v2.1 (waits on research-agent confirmation of current version)
- [ ] **E.2** `CONTRIBUTING.md` — PR sizing, branch naming, commit conventions, code-style refs, test reqs
- [ ] **E.3** `SECURITY.md` — disclosure channel + 90-day coordinated timeline
- [ ] **E.4** `.github/ISSUE_TEMPLATE/{bug_report,feature_request,config}.yml` — YAML templates
- [ ] **E.5** `.github/PULL_REQUEST_TEMPLATE.md` — single template with what/why/how/testing/checklist

### Wave F — Tier 3 pre-submission polish (~3 atomic commits)
- [ ] **F.1** README v1.1 — realistic JSON5 example + install troubleshooting section + FAQ additions
- [ ] **F.2** Architecture screenshot already in Wave C.4
- [ ] **F.3** OG-card link-unfurl verification via Playwright on opengraph.xyz or equivalent

### Wave G — Codex review on all new files (1 round, no commits unless findings)

### Wave H — Verification + push + close-out
- [ ] **H.1** `npm run type-check && npm run lint && npm test` after every Wave B commit
- [ ] **H.2** Playwright verify Observatory dashboard still renders after Wave B
- [ ] **H.3** Run `./scripts/check-ai-tone.sh --strict` on all new docs
- [ ] **H.4** Push all commits
- [ ] **H.5** Update Day 3 close-out user-side TODO + final summary

## Verification gates

- After Wave B: dashboard `?demo=1` still renders correctly via Playwright (UI polish must not break demo)
- After Wave C: each gallery image is real PNG + correct dimensions
- After Wave D: CHANGELOG.md passes ai-tone scan + keepachangelog format validates
- After Wave E: Contributor Covenant attribution complies with Creative Commons BY 4.0
- After Wave F: README still passes Mermaid render on github.com (Playwright)
- After Wave G: Codex SHIP verdict before final push

## Risk register

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| UI polish breaks `?demo=1` synthetic state | Med | Playwright check after each Wave B commit |
| Banana generates inconsistent aesthetic across 3 images | Med | Same exact-style prompt template for all 3; PIL re-encode + dimension check |
| Codex Day-3+1+1+1 finds a NEW HIGH bug | Low | At this audit depth, depth is good; act if found |
| Research agent finds Devvit policy change we'd missed | Low | Adjust submission strategy accordingly; not blocking |
| CHANGELOG includes stale "Day N" / "May 13 today" references | Med | Use ISO dates throughout, no relative phrasing |

## Done definition

- 11 Tier 1-3 items shipped (or formally deferred with rationale)
- Codex final review verdict ≥ SHIP-WITH-FIXES (all HIGH fixed before push)
- `scripts/check-ai-tone.sh --strict` passes on README + all new docs
- Observatory dashboard `?demo=1` still renders + Mermaid still renders post-polish
- Day 3 grand commit total exceeds 70 atomic
