# Tier 1-3 Final — Pre-Submission Polish + Recording Plan

> **STATUS: COMPLETE** — executed before 2026-05-14; preserved for audit trail. See `git log --oneline` for the commit sequence implementing this plan.

> Lock 2026-05-13 evening (Day 3 close-out). Stephen approved the 12-item Tier 1-3 menu. Zero overlap with Vinh's lane. Submission target May 20, hard deadline May 27.

## Tool inventory audit

Per locked memory [[tool-inventory-audit-per-task]]:

| Tool | Pass | Used? | Why |
|------|------|-------|-----|
| `codex:codex-rescue` sub-agent | Sub-agents | ✅ USED | Item 1 — pre-submission adversarial pass on the full paste-day bundle (writeup-draft + devpost-cheat + pillar-5 + new runbooks) |
| `last30days` skill | Skills | ✅ USED | Item 4 — weekly Devvit/policy intel refresh |
| `banana` skill | Skills | ✅ USED | Item 6 — 2 more 1200×800 3:2 gallery images |
| `plugin_playwright` MCP | MCP | ✅ USED | Verify gallery + Sparkline + EventRow changes render |
| `karpathy-guidelines` skill | Skills | ✅ USED | Think/simplify/surgical for UI polish |
| `frontend-design` skill | Skills | ✅ USED | Tier-A Phase-3 polish principles for items 5 |
| `caveman` skill | Skills | ✅ USED | Active |
| `three-brain` skill | Skills | ✅ USED | Codex hard-rule for "review" |

**SKIPPED with reason:**

| Tool | Skip reason |
|------|-------------|
| `firecrawl` MCP | No JS-rendered web scraping this wave; last30days script handles Reddit |
| `magic` MCP | Already-deferred per Phase 3 UI polish list (post-Phase-1 only) |
| `stitch` MCP | Same |
| `pr-review-toolkit:silent-failure-hunter` | No new bash this wave |
| `gemini-agent` | Item 1's Codex pass is the single-doc-bundle review; no multi-file cross-doc needed |
| `context7` MCP | No library docs queries |
| `concept-to-image` skill | Banana covers gallery image gen |
| `claude_ai_*` MCP | No new external surface (Notion/Slack/Gmail) |
| `plugin_supabase` / `vercel:*` | N/A |

## File structure

Files this plan creates or modifies:

- Create: `docs/submission/submission-day-runbook.md` — May 20 sequence (target) / May 27 (hard)
- Modify: `docs/submission/demo-video-runbook.md` — add synthetic-data fallback recording plan as bottom section
- Modify: `CHANGELOG.md` — [Unreleased] entries for this session (~89 commits added since the changelog was written)
- Modify: `src/client/components/Sparkline.tsx` — hover-interactivity (tooltip on bar hover)
- Modify: `src/client/components/EventRow.tsx` — scroll-into-view animation for newly-added rows
- Create: `assets/gallery-install.png` (1200×800 3:2) — install flow mockup
- Create: `assets/gallery-trigger.png` (1200×800 3:2) — rule-trigger close-up
- Create: `docs/superpowers/2026-05-13-research-deltas-week2.md` — last30days re-run output

## Wave sequence

### Wave A — Plan + memory (this commit) ✓

### Wave B — Tier 1 critical items (3 atomic commits)

- [ ] **B.1** `docs/submission/submission-day-runbook.md` — Stephen's May 20 target sequence: pre-flight AI-tone scan strict → Codex review → form fill → Devpost preview → publish gate → submit.
- [ ] **B.2** Synthetic-data demo recording plan as new section in `demo-video-runbook.md` — `?demo=1` capture path for if Phase 1 doesn't land by May 17. Caption-as-truth-telling per Devpost gallery research convention.

### Wave C — Tier 2 polish (5 atomic commits, content commits)

- [ ] **C.1** `CHANGELOG.md` `[Unreleased]` update — Tier 1-3 + audit + DESIGN.md + UI polish + Banana gallery + research-deltas all added.
- [ ] **C.2** `Sparkline.tsx` hover tooltip — show hour bucket + value on bar hover. Works on `?demo=1` synthetic data.
- [ ] **C.3** `EventRow.tsx` scroll-into-view animation — newly-added rows fade-up from bottom on next poll.
- [ ] **C.4** `assets/gallery-install.png` via Banana — install flow mockup (mod clicks "Add to community").
- [ ] **C.5** `assets/gallery-trigger.png` via Banana — single rule-trigger close-up (post fires regex rule → REMOVE + COMMENT chip surfaces).

### Wave D — Item 4 last30days intel refresh

- [ ] **D.1** Run `/last30days` on "Devvit hackathon" — fresh weekly intel before submission. Document in `docs/superpowers/2026-05-13-research-deltas-week2.md`.

### Wave E — Item 1 Codex pre-submission review

- [ ] **E.1** Dispatch `codex:codex-rescue` on the FULL paste-day bundle (writeup-draft + devpost-cheat + pillar-5 + submission-day-runbook + demo-video-runbook synthetic-data section + outreach-drafts).
- [ ] **E.2** Fix any HIGH/MED findings as atomic commits.

### Wave F — Final close-out

- [ ] **F.1** `npm run type-check && npm run lint && npm test` post UI polish.
- [ ] **F.2** Playwright verify Observatory dashboard `?demo=1` still renders.
- [ ] **F.3** Push all commits.
- [ ] **F.4** Final summary + Stephen's Tier 3 user-side action list.

## Risk register

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| Sparkline hover breaks demo rendering | Med | Playwright verify post-edit; test with empty + populated data |
| EventRow scroll-into-view animation churns on 10s poll cycle | Med | Apply animation only to newly-added rows (key-based detection), not all rows on every refresh |
| Banana gallery generations look inconsistent with existing 3 | Low | Use same prompt template + warm-charcoal aesthetic; reject if off-tone |
| last30days returns near-zero new signal | Low | OK — surface the "nothing new" finding as low-risk-confirmation |
| Codex E.1 finds a HIGH bug requiring rewrite | Med | Time-bound the fix: any HIGH lands within 2 commits, else defer to next session |
| Demo-runbook synthetic-data path conflicts with existing real-data script | Low | Add as new section, don't modify existing real-data instructions |

## Verification gates

- After Wave B: AI-tone scanner `--strict` passes on new runbook docs
- After Wave C: type-check + lint + 9 tests + vite build all pass; Playwright snapshot of `?demo=1` dashboard unchanged-modulo-new-animation
- After Wave D: last30days output saved to research-deltas-week2.md; high-priority findings (if any) actioned in atomic commits
- After Wave E: Codex final returns SHIP or SHIP-WITH-FIXES (all FIXES applied before close)
- After Wave F: all commits pushed; CI green

## Done definition

- 8-12 atomic commits shipped (3 Wave B + 5 Wave C + 1 Wave D + N from Wave E fixes + Wave F close-out)
- Submission day-of runbook is the single source of truth for Stephen's May 20 sequence
- Synthetic-data demo recording path documented in case Phase 1 slips
- 5 Devpost gallery candidates ready (target: install flow + rule trigger added to existing 3)
- Codex pre-submission review verdict ≥ SHIP-WITH-FIXES (all fixed)
- Day 3 grand commit total exceeds 100 atomic
